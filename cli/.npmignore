# Source TypeScript files — npm users get compiled JS in dist/, not the source
**/*.ts
!**/*.d.ts

# Test files
**/__tests__/
**/*.test.ts
**/*.spec.ts

# Build config (only needed during development)
tsup.config.ts
tsconfig.json

# Local dev artifacts
node_modules/
*.log
.DS_Store
